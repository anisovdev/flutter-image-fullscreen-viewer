name: fullscreen_image_viewer
description: A Flutter package that provides a customizable fullscreen image viewer with zoom and drag-to-dismiss features.
version: 0.0.2
homepage: https://github.com/anisovdev/flutter-image-fullscreen-viewer

environment:
  sdk: '>=3.1.5 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
